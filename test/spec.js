import 'jsdom-global/register';
import expect from 'expect';
import sinon from 'sinon';

import {
 reduxDeclarativeApi
} from '../src';
describe(`redux-declarative-api`, () => {

  it(`works`, () => {
     expect(true).toEqual(true);
  });

})
